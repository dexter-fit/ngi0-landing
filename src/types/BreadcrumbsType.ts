/**
 * Type for automatic header generation, can not be used outside app.
 */
export type BreadcrumbsType = {
    /**
     * Header item location link.
     */
    url: string,
    /**
     * Header item label.
     */
    label: string
}
