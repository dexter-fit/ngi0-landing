/**
 * Type for title page header content, can not be used outside app.
 */
export type TitleType = {
    /**
     * Main page title.
     */
    header: string,
    /**
     * Important questions.
     */
    highLights: string[],
    /**
     * Title page text.
     */
    mainText: string
}
