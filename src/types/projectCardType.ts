type ProjectCardType = {
    image: string;
    header: string;
    subheader: string;
    tags: string[];
    link: string;
    fund?: string;
    start?: string;
    end?: string;
};

export type {ProjectCardType};
